name: tests

on:
  push:
    branches: [main, "feature/**"]
  pull_request:

jobs:
  core-tests:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run dependency-free unit tests
        run: python -m unittest discover -s tests -v
      - name: Run golden CLI revision comparison
        run: python tools/revit_estimating.py compare tests/fixtures/baseline_snapshot.json tests/fixtures/current_snapshot.json --output .ci-output --json
      - name: Compile dependency-free core and tools
        run: python -m compileall -q lib tests tools
